# @grove/discord-bridge — environment
#
# Secrets are provisioned by Josh into 1Password `Grove Infra` (tracked in
# GOL-263) and injected at run time. NEVER commit real values. Left column =
# env var this app reads; right column = 1Password item key it maps to.

# Buffer (read-only insights token — X6cz). Already provisioned (GOL-226).
#   1P: Grove Infra / buffer_api_token
BUFFER_API_TOKEN=
# Buffer organization id — defaults to Goldberry Grove if unset.
BUFFER_ORG_ID=5a3295c5a73330590e63e1bb

# Discord bot (provisioned via GOL-263).
#   1P: Grove Infra / discord_bot_token
DISCORD_BOT_TOKEN=
#   1P: Grove Infra / discord_app_id
DISCORD_APP_ID=
#   1P: Grove Infra / discord_public_key   (Ed25519 request verification)
DISCORD_PUBLIC_KEY=
# Channel the weekly digest posts to. Per GOL-262 (Josh, 2026-07-22) the digest
# and the Phase 2 approval cards share one channel: #cmo-approvals (id
# 1527338819177938955), where the bot is already a member. A channel id is a
# Discord snowflake, not a secret — the digest workflow sets it inline. Kept here
# so the interactions server (loadConfig) has a value in local/dev runs.
DISCORD_WEEKLY_INSIGHTS_CHANNEL_ID=1527338819177938955

# Optional. Deep link used by the "Open in Buffer" button.
BUFFER_ANALYTICS_URL=https://publish.buffer.com

# Optional. Port the interactions server listens on (default 8787).
PORT=8787

# ── Phase 2: content-approval loop (GOL-470) ────────────────────────────────
# Channel the Approve/Revise/Reject cards post to. Per GOL-262 this is the same
# #cmo-approvals channel as the digest; defaults to
# DISCORD_WEEKLY_INSIGHTS_CHANNEL_ID when unset. Not a secret.
DISCORD_APPROVALS_CHANNEL_ID=1527338819177938955
# Discord user ids allowed to approve/revise/reject (comma-separated snowflakes).
# Macy (208085380262526976) is ALWAYS included in code; add Josh's id here.
#   Josh's Discord user id — fill at deploy.
DISCORD_APPROVER_IDS=
# Buffer draft-create uses the SAME token as read (cmo_buffer_key, validated in
# GOL-590) — no separate write token/env var is required.

# Audit `evt_` mirror to Paperclip (GOL-592, optional — degrades to stdout-only
# when unset). The bridge key is scoped to comment on the CMO audit issue.
#   1P: Grove Infra / paperclip_bridge_key
PAPERCLIP_BRIDGE_KEY=
# Issue the evt_ decision records are mirrored onto.
PAPERCLIP_AUDIT_ISSUE_ID=
# Paperclip API base for the mirror, e.g. http://localhost:3100
PAPERCLIP_API_BASE=
