// Empty stub for the `server-only` package, used as a vitest alias.
// In a real Next.js build, importing `server-only` from a Client Component
// throws; under vitest we simply do nothing so server modules can be tested.
export {};
