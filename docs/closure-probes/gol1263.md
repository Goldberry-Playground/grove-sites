# GOL-1263 closure-propagation live-acceptance probe

Automated acceptance probe for **GOL-289 / GOL-1206** — validates the
grove-sites inbound-close reconcile sweep end to end.

Round-trip under test:
1. Paperclip mirror-reconcile creates this issue's GitHub twin (outbound leg).
2. This PR merges with `Closes #<twin>` → GitHub closes the twin (GOL-149 path).
3. inbound-close-reconcile sweep detects the close and drives the Paperclip
   mirror (GOL-1263) to `done` in one cycle, with no re-open/bounce (loop guard).

Marker: gol1263-closeprop-roundtrip. Markdown-only, no code impact. Safe to revert/delete.
